{
  "name": "omni-copy",
  "description": "The ultimate text selection tool. Copy on select or via a button. Collect to an editable file. Control where it works — your clipboard, your rules.",
  "private": true,
  "version": "1.1.2",
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "dev:firefox": "wxt -b firefox",
    "build": "wxt build",
    "build:firefox": "wxt build -b firefox",
    "build:all": "npm run build && npm run build:firefox",
    "zip": "wxt zip",
    "zip:firefox": "wxt zip -b firefox",
    "zip:all": "npm run zip && npm run zip:firefox",
    "compile": "tsc --noEmit",
    "postinstall": "wxt prepare"
  },
  "devDependencies": {
    "@types/codemirror": "^5.60.18",
    "sass-embedded": "^1.99.0",
    "typescript": "^5.9.3",
    "wxt": "^0.20.25"
  },
  "dependencies": {
    "@codemirror/theme-one-dark": "^6.1.3",
    "codemirror": "^6.0.2"
  }
}
